{
  "name": "mcpilot",
  "version": "0.2.0",
  "description": "Local MCP proxy — one config, one connection, all your tools",
  "main": "dist/index.js",
  "bin": {
    "mcpilot": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "npm run build && node --experimental-strip-types tests/test-e2e.ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mcp", "proxy", "claude", "ai", "tools"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/selectqoma/mcpilot.git"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "commander": "^13.1.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3"
  }
}
